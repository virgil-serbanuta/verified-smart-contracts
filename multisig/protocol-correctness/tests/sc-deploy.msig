from address(7) run init(1, [address(7), .]);
from address(7) run proposeSCDeploy(10, bytes("code"), false, false, false, [.]);
from address(7) run sign(1);
from address(7) run performActionEndpoint(1);
